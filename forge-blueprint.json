{
  "schema_version": 1,
  "blueprint": {
    "id": "ibm-roks-bnk-roksbnkctl",
    "version": "1.0.0",
    "name": "IBM ROKS + BNK (roksbnkctl)",
    "description": "Provision an IBM Cloud ROKS cluster and install BIG-IP Next for Kubernetes (BNK) with roksbnkctl. Credential-backed by an IBM Cloud credential template; region and resource group inherit from the selected template."
  },
  "category": "bnk",
  "cloud_provider": "ibm",
  "icon": "server-stack",
  "color": "blue",
  "estimated_time": "30-45 minutes",
  "difficulty": "intermediate",
  "maturity": "preview",
  "tags": ["ibm", "roks", "openshift", "bnk", "roksbnkctl"],
  "outcomes": [
    "IBM Cloud ROKS (OpenShift) cluster provisioned",
    "BIG-IP Next for Kubernetes (BNK) installed",
    "Optional testing + gateway phases deployed"
  ],
  "inputs": {
    "required": [
      {
        "name": "prefix",
        "type": "string",
        "label": "Resource prefix",
        "description": "Prefix for all resource names (e.g. acme-eu).",
        "example": "acme-eu",
        "order": 1
      },
      {
        "name": "cluster_name",
        "type": "string",
        "label": "Cluster name (or existing name/ID)",
        "description": "Name for a new ROKS cluster, or the name/ID of an existing cluster to attach to.",
        "example": "acme-eu-roks",
        "order": 2
      },
      {
        "name": "region",
        "type": "string",
        "label": "IBM Cloud region",
        "description": "Region for the cluster. Inherits from the selected credential template.",
        "example": "eu-de",
        "source": "credential_template",
        "source_field": "region",
        "order": 3
      },
      {
        "name": "resource_group",
        "type": "string",
        "label": "Resource group",
        "description": "IBM Cloud resource group. Inherits from the selected credential template.",
        "example": "default",
        "source": "credential_template",
        "source_field": "ibmcloud_resource_group",
        "order": 4
      }
    ],
    "optional": [
      {
        "name": "cluster_create",
        "type": "boolean",
        "label": "Create a new cluster",
        "description": "On = provision a new ROKS cluster. Off = attach to the existing cluster named above.",
        "default": true,
        "order": 5
      },
      {
        "name": "openshift_version",
        "type": "string",
        "label": "OpenShift version",
        "description": "New cluster only. Blank = ROKS default.",
        "default": "",
        "order": 6
      },
      {
        "name": "workers_per_zone",
        "type": "number",
        "label": "Workers per zone",
        "description": "New cluster only. Worker nodes per zone.",
        "default": 2,
        "order": 7
      },
      {
        "name": "install_bnk",
        "type": "boolean",
        "label": "Install BNK",
        "description": "Install BIG-IP Next for Kubernetes.",
        "default": true,
        "order": 8
      },
      {
        "name": "testing_vpc",
        "type": "boolean",
        "label": "Testing: external client VPC",
        "description": "Deploy the testing phase's external client VPC + Transit Gateway jumphost.",
        "default": false,
        "order": 9
      },
      {
        "name": "testing_in_cluster",
        "type": "boolean",
        "label": "Testing: in-cluster jumphosts",
        "description": "Deploy the testing phase's in-cluster per-AZ jumphosts.",
        "default": false,
        "order": 10
      },
      {
        "name": "install_gateway",
        "type": "boolean",
        "label": "Deploy gateway phase",
        "description": "Deploy the gateway phase.",
        "default": false,
        "order": 11
      }
    ]
  },
  "modules": [
    {
      "id": "roksbnkctl-workspace",
      "module": "roksbnkctl/workspace",
      "version": "1.0.0",
      "name": "roksbnkctl Workspace",
      "optional": false,
      "depends_on": [],
      "inputs": {
        "prefix": "${prefix}",
        "cluster_name": "${cluster_name}",
        "region": "${region}",
        "resource_group": "${resource_group}",
        "cluster_create": "${cluster_create}",
        "openshift_version": "${openshift_version}",
        "workers_per_zone": "${workers_per_zone}",
        "install_bnk": "${install_bnk}",
        "testing_vpc": "${testing_vpc}",
        "testing_in_cluster": "${testing_in_cluster}",
        "install_gateway": "${install_gateway}"
      }
    }
  ],
  "is_featured": false
}
